# Commit History

Auto-generated from `git log` — do not hand-edit.

This file ships with placeholder content because Claude doesn't have local
git access to the real repo history or exact commit dates/times — only the
commit hashes and messages that showed up in `git diff`/`git push` output
during chat sessions. **Run this once after copying these files in, to
replace the placeholder below with the real, accurate log:**

```powershell
.\scripts\Update-CommitHistory.ps1
git add COMMIT_HISTORY.md
git commit -m "Add commit history log"
git push
```

From then on, re-run the same script after future pushes to keep it current.

---

## Known recent commits (from this session's chat log — dates unconfirmed)

- `10b021a` — Style the note photo upload as a dropzone
- `30f5ebe` — Add optional photo attachments to notes

Everything before `30f5ebe` (including the initial project history) isn't
listed here yet — the script above will backfill the complete, accurate log.
